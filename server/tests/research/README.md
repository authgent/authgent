# Research PoCs

Exploit demonstrations backing the chain-splicing / non-cascading
revocation research (private branch `research/chain-splicing-poc`).
Not part of the default CI regression suite's threat coverage claims —
run explicitly via `pytest tests/research/`.
